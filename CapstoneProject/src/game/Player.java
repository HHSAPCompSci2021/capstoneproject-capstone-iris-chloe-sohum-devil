package game;

public class Player {

	/*
	 * Maybe add a private field of type Orders to act as the thing the person is holding
	 * And add a public method to clear that field, so the hole can call it after something has been dumped into it
	 */
}
